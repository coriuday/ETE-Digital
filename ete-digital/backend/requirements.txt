# Core dependencies
fastapi==0.115.6
uvicorn[standard]>=0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10
greenlet==3.3.2


# Security
bcrypt==4.2.1
argon2-cffi==23.1.0
PyJWT==2.13.0
cryptography==49.0.0
python-multipart>=0.0.24
pyotp==2.9.0              # TOTP-based Two-Factor Authentication (RFC 6238)
google-auth==2.38.0       # Google OAuth token verification

# Redis — required for production (rate limiting across workers + WebSocket pub/sub)
redis==5.2.1
hiredis==3.1.0

# HTTP Client
httpx==0.28.1
dnspython==2.7.0

# Email
email-validator==2.2.0

# Object Storage
minio==7.2.11

# Background Tasks
apscheduler==3.10.4

# Rate Limiting
slowapi==0.1.9
limits==3.14.0

# Testing
pytest==9.1.1
pytest-asyncio==0.25.3
pytest-cov==6.0.0
aiosqlite==0.20.0

# Development
black==26.3.1
flake8==7.1.1
mypy==1.13.0

# AI Matching — Google Gemini (free tier LLM explanation layer)
google-generativeai>=0.8.0

# Monitoring
sentry-sdk[fastapi]==2.19.2

# Production server (gunicorn manages uvicorn workers for VPS/multi-core deployments)
# On Render, start.sh uses uvicorn directly; on VPS, gunicorn.conf.py is used.
gunicorn>=21.2.0,<24.0.0
