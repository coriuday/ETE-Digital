# ⚠️ This docker-compose.yml is outdated and has been superseded.
#
# The canonical development compose file is at the project root:
#   ete-digital/docker-compose.yml
#
# To start the full development stack, run from the project root:
#   docker compose up -d
#
# This file is kept only for historical reference and will be removed in a future cleanup.

# See: ete-digital/docker-compose.yml
